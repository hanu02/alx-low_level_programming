#include "main.h"
#include <stdlib.h>
/**
 * _abs - function that computes absolute value of an integer
 * return : The absolute value of an integer
 * @n : theinteger 
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
