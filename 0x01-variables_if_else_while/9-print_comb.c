#include <stdio.h>

/**
 * main - Prints all possible combinations of single-digit numbers.
 *
 * Return: Always 0.
 */
int main(void)
{
int num = 0;
if (num <= 9)
{
putchar(num);
putchar(',');
putchar(' ');
putchar ('\n');
}	
return (0);
}
